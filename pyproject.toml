[project]
name = "github-activity"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["requests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/github_activity"]